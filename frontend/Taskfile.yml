version: "3"

vars:
  VERSION: 1.0.2

tasks:
  dev:
    cmd: yarn run dev --host 0.0.0.0
  lint:
    cmd: yarn lint
  upgrade:
    cmd: yarn upgrade-interactive
  test:
    cmd: yarn run test:coverage --browser.headless
  test_watch:
    cmd: yarn run test:watch
  test_view:
    cmd: yarn vite preview --outDir test/unit --base /
  ### Build frontend
  build:
    dir: .
    cmds:
      - yarn
      - yarn vite build --outDir ../dist/static --emptyOutDir
      - yarn tsc -b
