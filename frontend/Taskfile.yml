version: '3'

vars:
  VERSION: 0.0.1

tasks:
  dev:
    cmd: yarn run dev --host 0.0.0.0
  ### Build frontend
  build:
    dir: ./frontend
    cmds:
      - yarn
      - yarn run build --outDir ../dist/static